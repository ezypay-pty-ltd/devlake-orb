description: >
  This command sends a post to devlake webhook indicating a deployment.
steps:
  - run:
      environment:
        GET_CONTEXT: "anything needed"
      name: Report deployment
      command: <<include(scripts/curl.sh)>>
